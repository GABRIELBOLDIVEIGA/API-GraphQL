const { gql } = require("apollo-server");

const userSchema = gql `
    type Query {
        users: [User]
    }

    type User {
        nome: String!
        ativo: Boolean!
        email: String
        role: Int
        id: Int
    }

`

module.exports = userSchema